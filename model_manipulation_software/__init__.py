"""
This module is used to generate predictions from a melloddy model.

To use it, you have to create a `PredictionSystem` object, and then call its `predict` function.

Example:
    You can see an example in `example.py` and run it with:

        $ python example.py



"""

from model_manipulation_software.prediction_system import PredictionSystem

__all__ = ["PredictionSystem"]
